---
# Manual run, lab reset or rollback: -e decom_fqdn=host.corp.example.com
- name: Decommission host
  hosts: localhost
  gather_facts: false

  vars:
    decom_fqdn: "{{ snow_fqdn }}"

  roles:
    - host_decommission
